name: flutter_dynamics
description: A new Flutter project.
publish_to: "none" # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: ">=2.18.5 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  mobx: ^2.3.3+2
  flutter_mobx: ^2.0.6+1
  mobx_codegen: ^2.0.7
  material: ^1.0.0+2
  dio: ^5.4.1
  equatable: ^2.0.5
  craftd_widget:
    path: ../craftd_widget

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  build_runner: ^2.4.8
flutter:
  uses-material-design: true
  assets:
    - assets/dynamic.json
